---
title: Intro
description: Landing page for the docs site. Replace this with whatever the project actually does.
slug: /
---

# Intro

This is the landing page for the docs site. Replace it with whatever the project actually is. A one-paragraph What. A short list of pages a new reader should hit first.

The site is set up so any MDX page can drop in MUI components without extra wiring. The theme is in `src/theme/mui/`; the `<ThemeProvider>` is already wrapped around every page in `src/theme/Root.tsx`.

## Setup

```bash
pnpm install
pnpm start
```

Dev server runs on `http://localhost:3000`. Hot reload picks up edits to `docs/`, `blog/`, and `src/`.
