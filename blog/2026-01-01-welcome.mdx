---
slug: welcome
title: Welcome
authors: [author]
tags: [meta]
---

The site is live. Docs live under `/docs` and the blog lives here.

{/* truncate */}

## Replacing this post

Edit `blog/2026-01-01-welcome.mdx` (or delete the file) and update `blog/authors.yml` with a real author entry. The blog directory can also be removed entirely if the site does not need one; drop the `blog` block from `docusaurus.config.ts` at the same time.
